## Online bilet satış sistemi

Laravel kurulumunu sırasında örnek veri yüklemek için;

```injectablephp
php artisan migrate --seed
```

eğer kurulum tamamlandıysa;
```injectablephp
php artisan db:seed
```

ile örnek veri yükleme işlemi yapabilirsiniz.
